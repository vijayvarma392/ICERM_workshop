language: python
python:
  - "3.6"
cache: pip
before_install:
  - sudo apt-get install -y libgsl-dev
  - pip install -r requirements.txt
install:
  - pip install .
addons:
  ssh_known_hosts:
      - bitbucket.org
